<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlkd.test.mapper.EmployeeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dlkd.test.pojo.Employee">
        <id column="id" property="id" />
        <result column="emp_name" property="empName" />
        <result column="sex" property="sex" />
        <result column="age" property="age" />
        <result column="dept_name" property="deptName" />
        <result column="emp_degree_name" property="empDegreeName" />
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, emp_name, sex, age, dept_name, emp_degree_name
    </sql>

</mapper>
